/* import { TestBed } from '@angular/core/testing';

import { RecipeByIdResolver } from './recipe-by-id.resolver';

describe('RecipeByIdResolver', () => {
  let resolver: RecipeByIdResolver;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    resolver = TestBed.inject(RecipeByIdResolver);
  });

  it('should be created', () => {
    expect(resolver).toBeTruthy();
  });
});
 */