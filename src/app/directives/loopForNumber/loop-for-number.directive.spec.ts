import { LoopForNumberDirective } from './loop-for-number.directive';

describe('LoopForNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new LoopForNumberDirective();
    expect(directive).toBeTruthy();
  });
});
